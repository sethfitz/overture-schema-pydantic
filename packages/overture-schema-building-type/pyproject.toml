[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "overture-schema-building-type"
version = "0.1.0"
description = "Pydantic schemas for Overture Maps building type"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0",
    "overture-schema-core",
    "overture-schema-buildings-common",
]

[tool.uv.sources]
overture-schema-core = { workspace = true }
overture-schema-buildings-common = { workspace = true }

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
    "mypy",
]


[tool.hatch.build.targets.wheel]
packages = ["src/overture"]