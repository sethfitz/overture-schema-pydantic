[project]
name = "overture-schema-addresses-theme"
version = "0.1.0"
description = "Overture Maps addresses theme shared structures"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "overture-schema-core",
    "overture-schema-address-type",
    "pydantic>=2.0",
]

[tool.uv.sources]
overture-schema-core = { workspace = true }
overture-schema-address-type = { workspace = true }


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/overture"]