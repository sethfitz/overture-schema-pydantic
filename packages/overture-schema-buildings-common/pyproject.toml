[project]
name = "overture-schema-buildings-common"
version = "0.1.0"
description = "Common structures shared across Overture Maps building types"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "overture-schema-core",
    "overture-schema-validation",
    "pydantic>=2.0",
]

[tool.uv.sources]
overture-schema-core = { workspace = true }
overture-schema-validation = { workspace = true }


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/overture"]
