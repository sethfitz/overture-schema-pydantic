# overture-schema-buildings-common

Common structures and enums shared across Overture Maps building types.

This package provides shared enums and models like `BuildingClass`, `FacadeMaterial`, `RoofMaterial`, etc. that are used by multiple building types (building, building_part).