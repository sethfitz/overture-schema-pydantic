# overture-schema-base-common

Common structures and enums shared across Overture Maps base theme feature types.

This package provides shared enums like `SurfaceMaterial` that are used by multiple base theme types (infrastructure, land, etc.).