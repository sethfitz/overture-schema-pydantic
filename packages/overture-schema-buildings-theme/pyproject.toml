[project]
name = "overture-schema-buildings-theme"
version = "0.1.0"
description = "Overture Maps buildings theme shared structures and building part types"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "overture-schema-core",
    "overture-schema-buildings-common",
    "overture-schema-building-type",
    "pydantic>=2.0",
]

[tool.uv.sources]
overture-schema-core = { workspace = true }
overture-schema-buildings-common = { workspace = true }
overture-schema-building-type = { workspace = true }


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/overture"]
