[project]
name = "overture-schema-land-cover-type"
version = "0.1.0"
description = "Land cover features representing Earth's natural surfaces"
requires-python = ">=3.10"
readme = "README.md"
dependencies = [
    "overture-schema-core",
    "overture-schema-base-common",
    "pydantic>=2.0",
]

[tool.uv.sources]
overture-schema-core = { workspace = true }
overture-schema-base-common = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=7.0",
    "ruff",
    "mypy",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.sdist]
include = [
  "/src",
]

[tool.hatch.build.targets.wheel]
packages = ["src/overture"]